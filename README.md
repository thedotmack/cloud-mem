# Cloud Mem

Public waitlist for [cloud-mem.ai](https://cloud-mem.ai) — AI memory in the cloud.

Static landing plus a Vercel serverless POST at `/api/waitlist` (Resend contacts).

Design source of record: `design-source/`. See `HANDOFF.md` for the next session.
