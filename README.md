# Cloud Mem

Public waitlist for [cloud-mem.ai](https://cloud-mem.ai) — memory that stays with you.

Static landing plus a Vercel serverless POST at `/api/waitlist` (Resend contacts).

See `HANDOFF.md` for the next session.
